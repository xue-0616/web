[package]
name = "relayer"
version.workspace = true
edition.workspace = true
[dependencies]
actix-web.workspace = true
sea-orm.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
anyhow.workspace = true
ethers.workspace = true
hex.workspace = true
chrono.workspace = true
thiserror.workspace = true
async-trait = "0.1"
deadpool-redis.workspace = true
tokio = { workspace = true, features = ["sync"] }
api = { path = "../api" }
daos-relayer = { path = "../daos-relayer" }
execute-validator = { path = "../execute-validator" }
contracts-abi = { path = "../contracts-abi" }
configs = { path = "../configs" }
# Needed for broadcaster::{build_stream_fields, push_to_stream}
# used by the POST /transactions/relay XADD push.
relayer-redis = { path = "../relayer-redis" }

[features]
# Enables `InMemoryReplayCache` for downstream integration tests that
# cannot spin up Redis. Unit tests inside this crate already get it
# for free via `#[cfg(test)]`.
test-support = []
