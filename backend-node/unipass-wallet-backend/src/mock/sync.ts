import { parsGooleUrl } from './config';

parsGooleUrl();
export const getChainSyncStatusInput = () => ({
    authChainNode: 'bsc-testnet',
});
export const getSyncTxInput = () => ({
    authChainNode: 'bsc-testnet',
});
