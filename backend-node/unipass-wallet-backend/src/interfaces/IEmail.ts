export enum TemplateType {
    code = "code",
    notify = "notify",
    policyRecoveryEmail = "policyRecoveryEmail",
    guardianRecoveryEmail = "guardianRecoveryEmail",
    syncAccount = "syncAccount",
    verifyUrl = "verifyUrl",
    accountInfo = "accountInfo",
}
