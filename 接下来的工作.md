1. CKB 工程师 — 实现 impl BatchTxBuilder
看： docs/scaffold-design.md §HIGH-FM-3（已写好所有契约）

第一个 PR（~400 行）：

读 ckb-types v0.116 + molecule 的 schema
写 FarmPoolCellData::from_bytes(&[u8]) -> Result<Self, InvalidInput>（反序列化 on-chain pool cell 数据结构）
单测 molecule 反序列化 round-trip + malformed input rejection
第二个 PR（~600 行）：

实现 CkbBatchTxBuilder::build()
收集 intent cells 作为 inputs，构造 output pool cell（更新 acc_reward_per_share），签名、send_transaction RPC 广播
返回 tx hash 给 scaffold，scaffold 自动标 Completed
第三个 PR（~200 行）：

在 integration-smoke 加 fixture：seed pending row → flip FARM_PROCESSING_ENABLED=true → poll 至 row 变 Completed
测试能在 CI 跑（需要 CKB devnet 容器 sidecar）
总工时估计： 3 个会话，需要 CKB 开发环境

2. 以太坊工程师 — 实现 impl TxBroadcaster + 重写 consume_once
看： docs/scaffold-design.md §MED-RL-3

第一个 PR（~300 行）：

写 NonceManager（内存 cache + eth_getTransactionCount 回退）
写 EthersBroadcaster：拿 SecurePrivateKey::as_bytes() → build EIP-1559 tx → eth_sendRawTransaction
错误映射：nonce too low → 查已广播的 hash 返回 Ok；insufficient funds → Transient；签名失败 → InvalidInput
第二个 PR（~200 行）：

重写 consume_once：XGROUP CREATE MKSTREAM → XREADGROUP → parse_stream_entry → process_entries → 循环 Vec<EntryAction> 做 XACK 或 skip
使用 integration-smoke workflow 验证 XACK 语义
第三个 PR（~100 行）：

集成测试：本地 Anvil fork，submit 一笔 meta-tx，验证链上可查到
总工时估计： 3 个会话，需要 Sepolia / Arbitrum testnet RPC + 一笔测试钱包

3. DevOps / SRE — 生产化部署
当前状态： 只有 docker-compose.integration.yml（本地演练用），没有任何生产部署配置

需要新建：

TLS 终结：Caddy / Nginx / AWS ALB 反代，证书管理（Let's Encrypt 或自签 + 内部 CA）
密钥管理：JWT_SECRET / RELAYER_PRIVATE_KEY / KEYBLOB_ENCRYPTION_KEY 进 Vault / AWS Secrets Manager / HSM，不再放 .env.integration
Kubernetes / Nomad 清单：Deployment + Service + Ingress，per-service Pod，readiness/liveness 探针已经在代码里（/health + /readyz）但 manifest 没写
监控栈：Prometheus + Grafana + AlertManager。服务已出 tracing 日志但没出 metrics——需要加 metrics-exporter-prometheus crate 并定义 SLO（latency p99、error rate、stream backlog size）
日志聚合：Loki / Elasticsearch / CloudWatch Logs，带 service、request_id、chain_id 索引
灾难恢复：MySQL 备份策略（pg_dump 等价物）、Redis RDB/AOF 配置、异地副本
on-call runbook：503 该怎么办、stream backlog 增长如何排查、replay cache 满了怎么办
总工时估计： 独立团队 2-4 周

4. 前端 / QA — Playwright E2E
当前状态： 后端 HTTP 契约有 guardrail-smoke.sh（11 断言）锁定，前端 ↔ 后端全链路无自动化

需要新建：

tests/e2e/ Playwright 项目
对 6 个前端 app 各写几个关键流程：登录、看 pool、查询价格、farm stake（后者在 FARM_PROCESSING_ENABLED=true 下会卡 Pending，这是预期的——测试得验证 UI 显示 "pending" 而不是 "success"）
CI job 用 headless Chromium 跑
总工时估计： 1-2 个会话 + 每个 app 单独跑

5. 运营 / 部署负责人 — 真实凭据
需要在生产环境填：

环境变量	来源
JWT_SECRET	随机 32+ 字节，进 Vault
RELAYER_PRIVATE_KEY	专用钱包私钥，HSM / Fireblocks / MPC
KEYBLOB_ENCRYPTION_KEY	随机 32 字节，进 Vault
FARM_ADMIN_ADDRESSES	真实运维团队的 CKB 地址列表（逗号分隔）
FARM_ADMIN_PUBKEYS	上述地址对应的 secp256k1 公钥
SEQUENCER_LOCK_CODE_HASH 等 5 个部署哈希	从实际部署到主网 / testnet 的合约 cell 里读出来
CORS_ALLOWED_ORIGINS	前端真实域名（多个）
总工时估计： 半天，但需要先完成 1-4