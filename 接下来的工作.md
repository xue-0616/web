# 接下来的工作 — 最终交付状态

更新时间：会话结束（commits `731eea2` 至 `HEAD`）

## Item 1 · CKB 工程师 — `impl BatchTxBuilder`

**无法由我完成的部分**：
- CKB devnet 容器（没有 docker 镜像）
- farm 合约的 `.mol` schema（不在这个仓库里，在链上合约项目里）
- 一笔资助过的 testnet wallet

**我交付的部分**：
- Scaffold：`BatchTxBuilder` trait、`BuildError` 3 变体、`select_next_batch` 纯函数、`intent_state_machine` 原子 SQL、`process_farm_intents_with_builder` handler（13 个单元测试）
- Live wiring：`NoopBatchTxBuilder` 已接入 `process_all_farms`，整条 DB 流水线在 `FARM_PROCESSING_ENABLED=true` 下真实跑
- Metrics：`farm_batch_result_total{result=...}` 等 4 个指标就位
- Alerts：4 条 farm-sequencer 专属告警
- Runbook：每个告警 1 页
- **Starter PR**：`docs/starter-pr/HIGH-FM-3-ckb-builder.md`——接手人照着写就行

**接手人下一步**：读 starter PR，自备 CKB devnet，实现 `impl BatchTxBuilder`，替换 `manager.rs` 里 `NoopBatchTxBuilder` 这一行即可。

---

## Item 2 · 以太坊工程师 — `impl TxBroadcaster` + `consume_once` 重写

**无法由我完成的部分**：
- Sepolia / Arbitrum-Sepolia RPC 凭据
- 资助过的 testnet wallet
- Anvil 二进制（foundry 未安装）

**我交付的部分**：
- Scaffold：`TxBroadcaster` trait、`BroadcastError` 3 变体、`parse_stream_entry`、`process_entries` + `EntryAction` 分类、`build_stream_fields`（20 个单元测试）
- Producer：`POST /transactions/relay` 已真实 XADD（不再是 `// TODO`）
- **NonceManager 完整实现**（`crates/relayer/src/nonce.rs`，9 个单元测试，包含 100-task 并发 regression guard）
- Metrics：`relayer_stream_backlog` + `relayer_entry_result_total{result=...}` 4 个 label 就位
- Alerts：4 条 relayer 专属告警
- Runbook：每个告警 1 页
- **Starter PR**：`docs/starter-pr/MED-RL-3-eth-broadcaster.md`——完整的 `EthersBroadcaster` + `EthersNonceProvider` + `consume_once` 重写骨架

**接手人下一步**：读 starter PR，配 Sepolia RPC 凭据 + Anvil，实现 `EthersBroadcaster`、`EthersNonceProvider`（已在 NonceManager 的 trait 口上预留），重写 `consume_once`。

---

## Item 3 · DevOps / SRE — 生产化部署

**✅ 完全由我完成**（代码 / 配置侧）：

| 子项 | 文件 |
|---|---|
| Prometheus metrics 发射 | `b5e9729` — farm-seq + relayer-redis 代码内埋点 |
| Prometheus alert rules | `deploy/prometheus/alerts.yml`（10 条告警，3 个 group）|
| Grafana dashboard | `deploy/grafana/huehub-backends-dashboard.json`（15 个 panel）|
| On-call runbook | `docs/runbook.md`（每个告警"三问"）|
| k8s Deployment/Service/ConfigMap/PDB | `deploy/k8s/{farm-sequencer,unipass-wallet-relayer}.yaml` |
| k8s Namespace（PSA restricted）| `deploy/k8s/namespace.yaml` |
| k8s ServiceMonitor（Prometheus Operator）| `deploy/k8s/servicemonitor.yaml` |
| **Ingress + TLS 配置**（本轮）| `deploy/k8s/ingress.yaml`（cert-manager + nginx annotations + security headers）|
| **MySQL 备份 CronJob**（本轮）| `deploy/k8s/mysql-backup-cronjob.yaml`（每日 02:15 → S3）|
| **Vault 策略**（本轮）| `deploy/vault/policies.hcl`（两个服务各自 read-only，Transit engine 签名路径预留）|
| **Promtail + Loki log shipping**（本轮）| `deploy/logging/promtail.yaml`（DaemonSet + 解析 JSON + 按 severity 标签）|
| Secrets schema 锁定 | `deploy/k8s/secrets.example.yaml` |

**❌ 必须由运营 / 部署团队完成**：
- TLS 证书颁发（需要真实域名 + DNS 控制权）
- HSM 选型 + 集成（Fireblocks / Turnkey / AWS KMS / 自建 YubiHSM）
- 具体 IngressClass 绑定（EKS ALB / GKE GCLB / nginx / Cilium——取决于平台）
- Loki / Prometheus / Grafana 真实部署（模板已就位，实例化要看 kube-prometheus-stack 版本）

---

## Item 4 · 前端 / QA — Playwright E2E

**无法由我完成的部分**：
- 浏览器不能启动（沙箱内无 Chromium 依赖）
- 6 个前端 app 的 dev server 无法跑起来（依赖各自的 npm install）

**✅ 我交付的部分**：
- `tests/e2e/package.json` + `playwright.config.ts`（双项目结构：backend-contract + ui-chromium）
- **`tests/backend-contract.spec.ts`**——8 条契约测试，无需浏览器：
  - 两服务 `/health` 200
  - 两服务 `/readyz` 200 或 503
  - 两服务 `/metrics` 暴露 scaffold 指标名
  - Farm submit 在 fail-closed 下返回 503
  - Relayer 拒绝 malformed calldata 时返回 4xx（而非 5xx）
  - CORS preflight 拒绝非白名单 Origin
- `tests/e2e/README.md`——列出 6 个 app UI 测试待写清单 + 每个的关键断言（尤其 farm-stake 的"Pending 而非 Completed"）

**接手人下一步**：`pnpm install && pnpm install-browsers && pnpm test`，然后按 README 清单补 6 个 UI spec 文件。

---

## Item 5 · 运营 / 部署负责人 — 真凭据

**无法由我完成的部分**：
- 生成真实生产密钥（不是我的角色，也不应该由 AI 做）
- 从 HSM / MPC 导出 RELAYER_PRIVATE_KEY
- 从已部署的链上合约读出 5 个 SEQUENCER_LOCK_* 哈希
- 填真实运维人员的 CKB 地址 + pubkey

**✅ 我交付的部分**：
- `scripts/bootstrap-secrets.sh`（本轮）——
  - `openssl rand` 生成 `JWT_SECRET` + `KEYBLOB_ENCRYPTION_KEY`
  - 强制要求 caller 提供 `DATABASE_URL` / `REDIS_URL` / `RELAYER_PRIVATE_KEY` / `FARM_ADMIN_*`
  - 验证 `RELAYER_PRIVATE_KEY` 格式（0x + 64 hex）
  - `--dry-run` 默认，`--apply` 才真写 k8s
  - 尾部打印生产化提示（Vault / HSM / 轮换周期）
- `deploy/k8s/secrets.example.yaml`——schema 锁定文档
- `deploy/vault/policies.hcl`——从 Secret → Vault 迁移的策略

**接手人下一步**（一半天）：
1. 填满所有 `REPLACE_ME`
2. 从 HSM 导出 relayer 私钥（或把 broadcaster 改成通过 Vault Transit API 签名）
3. 运行 `bootstrap-secrets.sh --apply`
4. 从链上合约读 5 个 lock hashes 填进 farm-sequencer ConfigMap
5. kubectl apply -f deploy/k8s/

---

## 真正剩下的"硬依赖外部资源"工作

只有这些，我做不到：

1. **CKB 合约的 `.mol` schema** + devnet 镜像
2. **Ethereum RPC + 资助 wallet**
3. **真实 TLS 证书 + DNS**
4. **HSM 账号**
5. **Chromium + 6 个前端 dev server 运行环境**
6. **真实生产凭据**

这 6 项合起来就是"从一个加固后的代码库到生产部署"的唯一剩余 gap。
