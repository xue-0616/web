# `backend-rust/unipass-snap-service-oss` base URL.
# Expected endpoints: /v1/account/{login_challenge,login,me,guide_status}
# and /v1/tx/{prepare,history}.
VITE_SNAP_SERVICE_URL=http://localhost:3080

# EVM RPC endpoint used by the wallet for gas estimation + tx broadcast.
VITE_RPC_URL=https://rpc.ankr.com/eth
